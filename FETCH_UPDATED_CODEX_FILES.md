Fetch the current `main` versions from `DreamMakers2/home-assistant-codex-bridge` of `CODEX_REFERENCE.md`, `codex/AGENTS.md`, `codex/config.example.toml`, `.codex/v2_lite-browser-ui-specialist.toml`, `client/install-codex-desktop-shortcut.sh`, `client/install-playwright-service.sh`, `docs/UBUNTU_RUNTIME.md`, `examples/HA_Scientific_Dashboard_Codex_Prompt.md`, `examples/HA_Statistics_Dashboard_Codex_Prompt.md`, `token_usage_analyzer/analyze.py`, `token_usage_analyzer/README.md`, and this prompt file; update the corresponding local bridge/workspace copies while preserving deployment-specific values such as `<HA_URL>`, merge the token-efficiency/MCP settings from `codex/config.example.toml` into `~/.codex/config.toml` without overwriting unrelated settings, re-run the desktop-launcher and Playwright-service installers as applicable, restart Codex so the settings take effect, verify the installed values, and report exactly what changed without reading or exposing secrets.
